# Jenny Art House 共用工作指示

本檔案適用於 Windows、MacBook，以及任何之後開啟本專案的 Codex 工作。

## 溝通習慣

- 預設用香港廣東話及繁體中文回覆，保持簡潔直接。
- 用戶通常會逐件提供產品：一至數張相片，加上一行產品名稱、尺寸、價錢及括號內分類。
- 收到這種產品資料時，視為要求把產品加入指定分類頁及產品詳情頁，而不只是整理文字。
- 保留用戶提供的產品名稱、尺寸、價格及分類；只統一空格和顯示格式，不自行改動實質資料。
- 有正面及背面相片時，產品詳情頁要同時提供兩張，列表卡片選用最清楚的正面相。
- 不要用 AI 改動產品本身的顏色、形狀或紋理，除非用戶明確要求修圖。
- 產品圖片檔案太大時要自動壓縮，不用每次再詢問。優先轉成適合網頁的高質 WebP／JPEG，按實際需要縮至約 2400px 長邊，保留原有色彩描述檔、產品色水及足夠放大細節；網站不可直接載入數十至數百 MB 的原始 TIF／JPEG。
- 若圖片路徑暫時讀不到，先在用戶提供的資料夾及專案資產內按檔名搜尋；找不到才說明，絕不可拿另一件貨的圖片代替。
- 用戶說「置頂」時，只置頂他所指的產品；現有其他固定排序要保留。
- 用戶說「更新」或「可以更新」時，表示可以立即完成檢查及正式部署。

## `(check)` 跨電腦同步指令

當用戶訊息最前面是 `(check)`（大小寫不拘）：

1. 在做同一段訊息後面的工作之前，先執行 `git fetch origin --prune`。
2. 檢查目前分支、`git status`、本機與 `origin/main` 的 ahead/behind，以及即將進來的檔案差異。
3. 工作樹乾淨時，用 fast-forward-only 方式同步 `main`，不可製造不必要的 merge commit。
4. 工作樹不乾淨時，不可直接覆蓋或丟棄檔案。先判斷現有修改是否已完成、是否與遠端重疊；可安全保存才提交／暫存，否則清楚報告衝突再處理。
5. 同步後再次確認本機 `HEAD` 與 `origin/main` 一致，才開始處理訊息餘下要求。
6. 向用戶簡短交代：有沒有拉到 MacBook 更新、同步到哪個提交，以及是否發現衝突。

`(check)` 的意思是「MacBook 已經把最新修改 push 到 GitHub，先同步再工作」，不是單純查看畫面。

## 網站修改流程

- 正式網站來源以本 Git repository 的 `main` 分支為準。
- 編輯前先了解現有 HTML、JavaScript、分類導覽及產品資料結構，沿用現有設計。
- 新增產品時至少同步檢查：
  - 正確分類頁的產品卡片；
  - 產品名稱、描述、標籤或鑲嵌資料包含 `18K`／`18 K`（大小寫不拘）時，除原本分類頁外，亦必須把同一產品卡加入 `jade-necklaces.html` 翡翠吊墜頁，並連到同一個 `product-detail.html?id=...` 詳情頁；
  - `product-detail.html` 的產品資料、正背圖片及 WhatsApp 查詢；
  - 中英文切換、頂部導覽及分類名稱；
  - 手機和桌面版顯示；
  - 圖片路徑、產品卡片點擊及 WhatsApp 連結。
- 新產品文案可以簡短描述實物及寓意，但不可虛構鑑定、產地證明、材質等級或療效。
- 價格統一顯示為 `HK$ 12,800 | 請查詢` 類型；尺寸按用戶提供的毫米數值保存。
- 修改後要做相稱的本機驗證；涉及版面或圖片時要實際用瀏覽器檢查手機及桌面畫面。
- 除非用戶叫停或要求只做本機版本，完成網站工作後要：
  1. 部署到現有 Cloudflare Workers 網站；
  2. 檢查正式網址；
  3. 只提交本次網站相關檔案；
  4. push 到 `origin/main`，讓另一部電腦下次 `(check)` 可以同步。

## Git 及安全規則

- GitHub repository：`MomomaiHimeki/JennyArtHouse`，主要分支：`main`。
- 不可把對話紀錄、個人備份、壓縮輸出、憑證、token 或本機專用路徑提交到 GitHub。
- 不可使用 `git reset --hard`、強制 push，或丟棄未確認的用戶修改。
- 每次提交前檢查 staged files，確保沒有誤收大型或私密檔案。
- 如果 MacBook 和 Windows 同時修改同一檔案，先保留兩邊內容並解決衝突，不能任意選一邊覆蓋。
